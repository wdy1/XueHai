@model IEnumerable<Models.GoodsK>

@{
    ViewBag.Title = "Type";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Type</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GoodsKName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GoodsKName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GoodsK_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.GoodsK_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GoodsK_id })
        </td>
    </tr>
}

</table>
